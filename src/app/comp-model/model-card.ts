export class card{
  image:string;
  textcontent:string;
  titleContent:string

  constructor(image:string, textcontent:string, titleContent:string){
    this.image = image;
    this.textcontent = textcontent;
    this.titleContent = titleContent;
  }
}
